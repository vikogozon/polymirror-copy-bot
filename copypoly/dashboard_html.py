LICENSE_HTML = r"""<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>PolyMirror — Get Access</title>
<style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#060a0f;color:#c8d8e8;font-family:'Courier New',monospace;min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px}
.wrap{width:100%;max-width:520px}
.logo{font-size:22px;letter-spacing:8px;color:#00ff88;text-shadow:0 0 20px rgba(0,255,136,0.5);text-align:center;margin-bottom:4px}
.sub{text-align:center;font-size:10px;letter-spacing:4px;color:#4a6a7a;margin-bottom:28px}
.buy-box{background:linear-gradient(135deg,#0a1a12,#0a1520);border:1px solid rgba(0,255,136,0.35);border-radius:8px;padding:30px 32px;margin-bottom:18px}
.headline{font-size:15px;color:#e0f0e8;letter-spacing:1px;line-height:1.7;text-align:center;margin-bottom:6px}
.headline strong{color:#00ff88}
.subhead{font-size:11px;color:#5a8a6a;letter-spacing:1px;text-align:center;margin-bottom:24px;line-height:1.6}
.divider{border:none;border-top:1px solid #0f2018;margin:20px 0}
.features{list-style:none;margin-bottom:24px}
.features li{font-size:11px;color:#7aaab0;letter-spacing:1px;padding:5px 0;display:flex;align-items:flex-start;gap:8px}
.features li .ck{color:#00ff88;flex-shrink:0}
.features li .ft{color:#4a7a6a;font-size:10px;display:block;margin-top:1px}
.price-row{text-align:center;margin-bottom:8px}
.price{font-size:40px;color:#00ff88;text-shadow:0 0 24px rgba(0,255,136,0.4);letter-spacing:2px}
.price-note{font-size:10px;color:#3a6a4a;letter-spacing:2px;margin-top:2px}
.anchor{font-size:10px;color:#3a5a4a;letter-spacing:1px;text-align:center;margin-bottom:20px}
.btn-buy{display:block;width:100%;padding:16px;background:linear-gradient(90deg,rgba(0,255,136,0.14),rgba(0,255,136,0.08));border:1px solid #00ff88;border-radius:4px;color:#00ff88;font-family:'Courier New',monospace;font-size:12px;letter-spacing:4px;cursor:pointer;text-decoration:none;transition:all .25s;text-align:center;margin-bottom:10px}
.btn-buy:hover{background:linear-gradient(90deg,rgba(0,255,136,0.26),rgba(0,255,136,0.14));box-shadow:0 0 28px rgba(0,255,136,0.28)}
.guarantee{font-size:10px;color:#2a5a3a;letter-spacing:1px;text-align:center;line-height:1.6}
.act-box{background:#0a1520;border:1px solid #1a2a3a;border-radius:8px;padding:24px 32px}
.act-title{font-size:10px;letter-spacing:3px;color:#3a5a6a;text-align:center;margin-bottom:18px}
label{display:block;font-size:10px;letter-spacing:2px;color:#3a5a6a;margin-bottom:8px}
input{width:100%;background:#060a0f;border:1px solid #1a2a3a;border-radius:4px;color:#c8d8e8;font-family:'Courier New',monospace;font-size:13px;padding:12px;letter-spacing:2px;outline:none;transition:border-color .2s}
input:focus{border-color:#00ff88;box-shadow:0 0 0 2px rgba(0,255,136,0.08)}
input::placeholder{color:#1a3a4a;letter-spacing:1px}
.btn-act{width:100%;margin-top:14px;padding:13px;background:rgba(0,229,255,0.05);border:1px solid rgba(0,229,255,0.3);border-radius:4px;color:#00e5ff;font-family:'Courier New',monospace;font-size:11px;letter-spacing:4px;cursor:pointer;transition:all .2s}
.btn-act:hover{background:rgba(0,229,255,0.10)}
.btn-act:disabled{opacity:0.4;cursor:not-allowed}
.msg{margin-top:12px;font-size:11px;text-align:center;min-height:18px;letter-spacing:1px}
.msg.ok{color:#00ff88}.msg.err{color:#ff4455}
.hint{margin-top:14px;font-size:10px;color:#1a3a4a;text-align:center;letter-spacing:1px;line-height:1.7}
</style>
</head>
<body>
<div class="wrap">
  <div class="logo">◈ POLYMIRROR</div>
  <div class="sub">POLYMARKET COPY TRADING SYSTEM</div>

  <div class="buy-box">
    <div class="headline">
      While you're reading this,<br>
      <strong>profitable traders are placing their bets.</strong>
    </div>
    <div class="subhead">
      PolyMirror copies their trades automatically — in real time,<br>on your machine, with your money, under your control.
    </div>
    <hr class="divider">
    <ul class="features">
      <li><span class="ck">✓</span><div><span>Copies any Polymarket wallet in real time</span><span class="ft">The moment they trade, you trade.</span></div></li>
      <li><span class="ck">✓</span><div><span>Live dashboard with P&amp;L tracking</span><span class="ft">See every copy, every fill, every dollar.</span></div></li>
      <li><span class="ck">✓</span><div><span>Risk controls built in</span><span class="ft">Set per-trade limits, cooldowns, and size caps.</span></div></li>
      <li><span class="ck">✓</span><div><span>Real-time trade mirroring</span><span class="ft">Copies positions the moment the trader places them.</span></div></li>
      <li><span class="ck">✓</span><div><span>Runs entirely on your machine</span><span class="ft">Your keys never leave your computer. No cloud, no SaaS.</span></div></li>
    </ul>
    <hr class="divider">
    <div class="price-row">
      <div class="price">$25</div>
      <div class="price-note">ONE-TIME · LIFETIME ACCESS · NO SUBSCRIPTION</div>
    </div>
    <div class="anchor">One profitable copied trade pays for this tool.</div>
    <br>
    <a class="btn-buy" href="https://buy.stripe.com/aFafZg5j827D5yTcZp5kk00" target="_blank">⚡ GET INSTANT ACCESS — $25 USD</a>
    <div class="guarantee">After payment you receive your license key by email within minutes.</div>
  </div>

  <div class="act-box">
    <div class="act-title">ALREADY PURCHASED? ENTER YOUR KEY BELOW</div>
    <label>LICENSE KEY</label>
    <input id="keyInput" type="text" placeholder="POLY-XXXX-XXXX-XXXX-XXXX" autocomplete="off" spellcheck="false"
      onkeydown="if(event.key==='Enter')activate()">
    <button class="btn-act" id="btn" onclick="activate()">ACTIVATE LICENSE</button>
    <div class="msg" id="msg"></div>
    <div class="hint">Paste your key above and click ACTIVATE LICENSE.<br>The dashboard unlocks instantly.</div>
  </div>
</div>
<script>
async function activate() {
  const key = document.getElementById('keyInput').value.trim();
  if (!key) return;
  const btn = document.getElementById('btn');
  const msg = document.getElementById('msg');
  btn.disabled = true;
  btn.textContent = 'VALIDATING...';
  msg.className = 'msg';
  msg.textContent = 'Connecting to license server...';
  try {
    const r = await fetch('/api/license', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({key})
    });
    const j = await r.json();
    if (j.ok) {
      msg.className = 'msg ok';
      msg.textContent = '✓ ' + j.message;
      setTimeout(() => { window.location.href = '/'; }, 1200);
    } else {
      msg.className = 'msg err';
      msg.textContent = '✗ ' + j.message;
      btn.disabled = false;
      btn.textContent = 'ACTIVATE LICENSE';
    }
  } catch(_) {
    msg.className = 'msg err';
    msg.textContent = '✗ Connection failed. Try again.';
    btn.disabled = false;
    btn.textContent = 'ACTIVATE LICENSE';
  }
}
</script>
</body>
</html>"""

MAIN_HTML = r"""<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>POLYMIRROR - Copy Trading</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;600&family=Share+Tech+Mono&display=swap" rel="stylesheet">
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --bg:#04040f;--bg2:#080818;--bg3:#0c0c20;
  --cyan:#00e5ff;--cyan-dim:rgba(0,229,255,0.10);
  --green:#00ff88;--red:#ff4455;--yellow:#ffd700;
  --text:#cce4f8;--muted:#5a80a0;--label:#8ab4d4;
  --border:rgba(0,229,255,0.18);
  --border2:rgba(0,229,255,0.08);
  --glow-c:0 0 24px rgba(0,229,255,0.5);
  --glow-g:0 0 24px rgba(0,255,136,0.5);
  --glow-r:0 0 24px rgba(255,68,85,0.5);
}
body{background:var(--bg);color:var(--text);font-family:'Rajdhani',sans-serif;height:100vh;overflow:hidden;display:flex;flex-direction:column}
body::after{content:'';position:fixed;inset:0;background:repeating-linear-gradient(0deg,transparent,transparent 2px,rgba(0,0,0,0.06) 2px,rgba(0,0,0,0.06) 4px);pointer-events:none;z-index:9999}
#mx{position:fixed;inset:0;z-index:0;opacity:0.45;pointer-events:none}
.grid-bg{position:fixed;inset:0;background-image:linear-gradient(rgba(0,229,255,0.05) 1px,transparent 1px),linear-gradient(90deg,rgba(0,229,255,0.05) 1px,transparent 1px);background-size:60px 60px;pointer-events:none;z-index:1}
.hdr{display:flex;align-items:center;justify-content:space-between;padding:13px 22px;background:rgba(4,4,16,0.85);border-bottom:1px solid var(--border);backdrop-filter:blur(16px);position:relative;z-index:10;flex-shrink:0}
.logo{font-family:'Orbitron',sans-serif;font-size:22px;font-weight:900;letter-spacing:6px;color:var(--cyan);text-shadow:var(--glow-c)}
.logo em{color:#fff;font-style:normal;text-shadow:0 0 18px rgba(255,255,255,0.4)}
.hdr-right{display:flex;align-items:center;gap:18px}
.uptime{font-family:'Share Tech Mono',monospace;font-size:20px;color:var(--cyan);letter-spacing:3px;min-width:100px;text-align:right;text-shadow:0 0 10px rgba(0,229,255,0.5)}
.badge{display:flex;align-items:center;gap:7px;padding:5px 14px;border-radius:3px;font-family:'Orbitron',sans-serif;font-size:10px;letter-spacing:2px}
.badge.running{background:rgba(0,255,136,0.10);border:1px solid var(--green);color:var(--green);box-shadow:0 0 12px rgba(0,255,136,0.2)}
.badge.stopped{background:rgba(255,68,85,0.10);border:1px solid var(--red);color:var(--red)}
.dot{width:7px;height:7px;border-radius:50%;background:currentColor}
.running .dot{box-shadow:var(--glow-g);animation:blink 1.5s infinite}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.2}}
.main{display:grid;grid-template-columns:1fr 348px;flex:1;overflow:hidden;position:relative;z-index:2}
.left{display:flex;flex-direction:column;gap:13px;padding:14px;overflow:hidden}
.right{border-left:1px solid var(--border);background:rgba(4,4,16,0.6);display:flex;flex-direction:column;overflow:hidden;backdrop-filter:blur(8px)}
.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;flex-shrink:0}
.card{background:rgba(4,4,14,0.55);border:1px solid var(--border);border-radius:6px;padding:13px 15px;position:relative;overflow:hidden;backdrop-filter:blur(6px)}
.card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,var(--cyan),transparent);animation:scanline 3s ease-in-out infinite}
@keyframes scanline{0%,100%{opacity:.3}50%{opacity:1}}
.card-label{font-size:10px;letter-spacing:2.5px;text-transform:uppercase;color:var(--label);margin-bottom:5px;font-weight:600}
.card-val{font-family:'Orbitron',sans-serif;font-size:26px;font-weight:700;color:var(--cyan);text-shadow:var(--glow-c)}
.card-val.g{color:var(--green);text-shadow:var(--glow-g)}
.card-val.r{color:var(--red);text-shadow:var(--glow-r)}
.card-total .card-val{font-size:30px}
.card-sub{font-size:11px;color:var(--muted);margin-top:3px;font-family:'Share Tech Mono',monospace;letter-spacing:.5px}
.feed-wrap{flex:1;background:rgba(4,4,14,0.12);border:1px solid var(--border);border-radius:6px;display:flex;flex-direction:column;overflow:hidden}
.feed-hdr{font-family:'Orbitron',sans-serif;font-size:10px;letter-spacing:3px;color:var(--cyan);padding:11px 14px;border-bottom:1px solid var(--border);flex-shrink:0;background:rgba(0,0,0,0.35);text-shadow:var(--glow-c)}
.feed{flex:1;overflow-y:auto;display:flex;flex-direction:column;gap:4px;padding:8px;background:transparent}
.feed-empty{color:var(--muted);font-size:13px;text-align:center;padding:50px 20px;letter-spacing:1px}
.trow{display:grid;grid-template-columns:52px 44px 68px 1fr 70px;gap:7px;align-items:center;padding:10px 10px;border-radius:4px;border:1px solid transparent;font-size:14px;animation:fadeIn .3s ease}
@keyframes fadeIn{from{opacity:0;transform:translateX(-10px)}to{opacity:1;transform:translateX(0)}}
.trow.filled,.trow.matched{background:rgba(0,255,136,0.07);border-color:rgba(0,255,136,0.25);border-left:3px solid var(--green)}
.trow.skipped{background:rgba(0,0,0,0.25);border-color:rgba(255,255,255,0.06);border-left:3px solid var(--muted);opacity:.6}
.trow.error{background:rgba(255,68,85,0.07);border-color:rgba(255,68,85,0.25);border-left:3px solid var(--red)}
.t-time{color:var(--muted);font-family:'Share Tech Mono',monospace;font-size:12px}
.t-buy{color:var(--green);font-weight:700;text-shadow:0 0 8px rgba(0,255,136,0.6)}
.t-sell{color:var(--red);font-weight:700;text-shadow:0 0 8px rgba(255,68,85,0.6)}
.t-out{color:var(--cyan);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.t-mkt{color:var(--text);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.t-amt{color:var(--yellow);font-weight:600;text-align:right;font-family:'Share Tech Mono',monospace;font-size:11px;text-shadow:0 0 8px rgba(255,215,0,0.4)}
.cfg-hdr{font-family:'Orbitron',sans-serif;font-size:10px;letter-spacing:3px;color:var(--cyan);padding:13px 17px;border-bottom:1px solid var(--border);flex-shrink:0;background:rgba(0,229,255,0.03);text-shadow:var(--glow-c)}
.cfg-body{flex:1;overflow-y:auto;padding:13px 17px;display:flex;flex-direction:column;gap:12px}
.fg label{display:block;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--label);margin-bottom:5px;font-weight:600}
.fg label span{font-size:9px;letter-spacing:0;text-transform:none;color:var(--muted)}
.fg input,.fg select{width:100%;background:rgba(0,229,255,0.06);border:1px solid rgba(0,229,255,0.2);border-radius:4px;color:var(--text);padding:8px 11px;font-family:'Rajdhani',sans-serif;font-size:14px;font-weight:500;outline:none;transition:all .2s}
.fg input::placeholder{color:var(--muted)}
.fg input:focus,.fg select:focus{border-color:var(--cyan);box-shadow:0 0 12px rgba(0,229,255,0.15);background:rgba(0,229,255,0.09)}
.fg select option{background:var(--bg2)}
.mrow{display:flex;gap:7px}
.toggle-wrap{display:flex;align-items:center;gap:10px;padding:2px 0}
.toggle-label{font-size:11px;font-weight:600;letter-spacing:.5px;transition:color .25s;white-space:nowrap;cursor:pointer;user-select:none}
.toggle-label.active{color:var(--cyan);text-shadow:0 0 10px rgba(0,229,255,0.5)}
.toggle-label.inactive{color:var(--muted)}
.toggle-track{position:relative;width:46px;height:24px;border-radius:12px;background:rgba(0,229,255,0.08);border:1px solid rgba(0,229,255,0.25);cursor:pointer;transition:all .25s;flex-shrink:0}
.toggle-track.pct{background:rgba(0,229,255,0.14);border-color:var(--cyan);box-shadow:0 0 12px rgba(0,229,255,0.2)}
.toggle-knob{position:absolute;top:3px;left:3px;width:16px;height:16px;border-radius:50%;background:var(--muted);transition:all .25s;box-shadow:0 1px 4px rgba(0,0,0,0.4)}
.toggle-track.pct .toggle-knob{left:25px;background:var(--cyan);box-shadow:0 0 8px rgba(0,229,255,0.7),0 1px 4px rgba(0,0,0,0.3)}
.mode-status{font-size:10px;letter-spacing:.5px;margin-top:5px;padding:5px 8px;border-radius:3px;border:1px solid rgba(0,229,255,0.12);background:rgba(0,229,255,0.04);color:var(--label);font-family:'Share Tech Mono',monospace}
.btn{width:100%;padding:11px;border-radius:4px;border:none;font-family:'Orbitron',sans-serif;font-size:10px;letter-spacing:2px;cursor:pointer;transition:all .25s;font-weight:700}
.btn-start{background:rgba(0,255,136,0.08);border:1px solid var(--green);color:var(--green);font-size:11px;letter-spacing:3px}
.btn-start:hover{background:rgba(0,255,136,0.16);box-shadow:var(--glow-g),inset 0 0 20px rgba(0,255,136,0.05)}
.btn-stop{background:rgba(255,68,85,0.08);border:1px solid var(--red);color:var(--red);font-size:11px;letter-spacing:3px}
.btn-stop:hover:not(:disabled){background:rgba(255,68,85,0.16);box-shadow:var(--glow-r)}
.btn-stop:disabled{opacity:0.25;cursor:not-allowed}
.btn-start:disabled{opacity:0.25;cursor:not-allowed}
.divider{height:1px;background:linear-gradient(90deg,transparent,var(--border),transparent);margin:2px 0}
.toast{position:fixed;bottom:20px;right:20px;padding:10px 18px;border-radius:4px;font-size:13px;font-weight:600;z-index:9998;animation:toastIn .3s ease;border:1px solid var(--green);background:rgba(0,255,136,0.10);color:var(--green);box-shadow:var(--glow-g);letter-spacing:.5px}
.toast.err{border-color:var(--red);background:rgba(255,68,85,0.10);color:var(--red);box-shadow:var(--glow-r)}
@keyframes toastIn{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}
.con-hdr{font-family:'Orbitron',sans-serif;font-size:9px;letter-spacing:3px;color:var(--label);padding:8px 17px 5px;border-top:1px solid var(--border);flex-shrink:0;background:rgba(0,229,255,0.02)}
.con{height:260px;overflow-y:auto;padding:4px 17px 10px;font-family:'Share Tech Mono',monospace;font-size:11px;color:var(--label);line-height:1.8;flex-shrink:0}
.conn-dot{width:6px;height:6px;border-radius:50%;background:var(--muted);display:inline-block;margin-right:5px}
.conn-dot.ok{background:var(--green);box-shadow:var(--glow-g)}
::-webkit-scrollbar{width:3px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:rgba(0,229,255,0.2);border-radius:2px}
</style>
</head>
<body>
<canvas id="mx"></canvas>
<div class="grid-bg"></div>
<header class="hdr">
  <div class="logo">POLY<em>MIRROR</em></div>
  <div class="hdr-right">
    <div class="uptime" id="uptime"></div>
    <div class="badge stopped" id="badge">
      <div class="dot"></div>
      <span id="badgeText">STOPPED</span>
    </div>
    <span><span class="conn-dot" id="connDot"></span><span id="connText" style="font-size:10px;color:var(--label);letter-spacing:1px">connecting</span></span>
  </div>
</header>
<div class="main">
  <div class="left">
    <div class="metrics">
      <div class="card card-total">
        <div class="card-label">Total Portfolio</div>
        <div class="card-val" id="mTotal" style="color:var(--cyan)">—</div>
        <div class="card-sub" id="mTotalSub"></div>
      </div>
      <div class="card">
        <div class="card-label">Cash</div>
        <div class="card-val" id="mBalance" style="color:var(--yellow);text-shadow:0 0 20px rgba(255,215,0,0.5)">—</div>
      </div>
      <div class="card">
        <div class="card-label">Positions</div>
        <div class="card-val" id="mPortfolio">—</div>
      </div>
      <div class="card">
        <div class="card-label">Trades Copied</div>
        <div class="card-val g" id="mCopied">0</div>
      </div>
      <div class="card">
        <div class="card-label">Session Invested</div>
        <div class="card-val" id="mSpent">$0.00</div>
      </div>
      <div class="card">
        <div class="card-label">Unrealized P&amp;L</div>
        <div class="card-val" id="mPnl">—</div>
      </div>
    </div>
    <div class="feed-wrap">
      <div class="feed-hdr">◈ LIVE TRADE FEED</div>
      <div class="feed" id="feed">
        <div class="feed-empty">Waiting for the first trade...</div>
      </div>
    </div>
  </div>
  <div class="right">
    <div class="cfg-hdr">CONFIGURATION</div>
    <div class="cfg-body">
      <div class="fg">
        <label>Wallet to Copy</label>
        <input id="cfgWallet" type="text" placeholder="0x..." onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="walletStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="fg">
        <label>Trade Sizing Mode</label>
        <div class="toggle-wrap">
          <span class="toggle-label active" id="mFixed" onclick="setModeAndSave('fixed')">Fixed USD</span>
          <div class="toggle-track" id="toggleTrack" onclick="setModeAndSave(mode==='fixed'?'percent':'fixed')">
            <div class="toggle-knob" id="toggleKnob"></div>
          </div>
          <span class="toggle-label inactive" id="mPct" onclick="setModeAndSave('percent')">% Balance</span>
        </div>
        <div class="mode-status" id="modeStatus">MODE: Fixed $5.00 per trade</div>
      </div>
      <div class="fg" id="rowFixed">
        <label>Amount per Trade (USD)</label>
        <input id="cfgFixed" type="number" min="1" step="1" placeholder="5" oninput="updateModeStatus()" onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="fixedStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="fg" id="rowPct" style="display:none">
        <label>% of Cash Balance per Trade <span style="color:var(--muted);font-size:9px;letter-spacing:0">— reads live balance automatically</span></label>
        <input id="cfgPct" type="number" min="0.1" max="100" step="0.1" placeholder="2" oninput="updateModeStatus()" onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="pctStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="fg">
        <label>Ignore Trades Older Than (seconds)</label>
        <input id="cfgLookback" type="number" min="60" step="60" placeholder="300" onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="lookbackStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="fg">
        <label>Market Cooldown (seconds) <span style="color:var(--muted);font-size:9px;letter-spacing:0">— min time between buys on the same market. 0 = off</span></label>
        <input id="cfgCooldown" type="number" min="0" step="10" placeholder="0" onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="cooldownStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="divider"></div>
      <div class="fg">
        <label>Private Key <span style="color:var(--muted);font-size:9px;letter-spacing:0">— from your wallet (MetaMask → Account Details → Export Private Key)</span></label>
        <input id="cfgPrivKey" type="password" placeholder="0x..." autocomplete="off" onfocus="if(this.dataset.saved==='1'){this.value='';this.dataset.saved='0';}" onblur="autoSaveCreds()">
        <div id="pkStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="fg">
        <label>Polymarket Address (Funder) <span style="color:var(--muted);font-size:9px;letter-spacing:0">— your 0x address on polymarket.com</span></label>
        <input id="cfgFunder" type="text" placeholder="0x..." onblur="autoSaveConfig()" onkeydown="if(event.key==='Enter')autoSaveConfig()">
        <div id="funderStatus" style="font-size:11px;margin-top:4px;color:var(--muted)"></div>
      </div>
      <div class="divider"></div>
      <button class="btn btn-start" id="btnStart" onclick="startBot()">START BOT</button>
      <button class="btn btn-stop" id="btnStop" onclick="stopBot()" disabled>STOP BOT</button>
    </div>
    <div class="con-hdr">SYSTEM LOG</div>
    <div class="con" id="con"></div>
  </div>
</div>
<script>
(function(){
  const cv=document.getElementById('mx');const cx=cv.getContext('2d');
  const CHARS='01アイウエオカキクケコサシスセソタチツテトナニヌネノ0123456789ABCDEF◈▶◆Ξ$₿×≡';
  const FS=13;let cols,drops,speeds;
  function resize(){cv.width=window.innerWidth;cv.height=window.innerHeight;cols=Math.floor(cv.width/FS);drops=Array.from({length:cols},()=>Math.random()*-80);speeds=Array.from({length:cols},()=>0.3+Math.random()*0.7);}
  function draw(){cx.fillStyle='rgba(4,4,15,0.18)';cx.fillRect(0,0,cv.width,cv.height);cx.font=FS+'px "Share Tech Mono",monospace';for(let i=0;i<cols;i++){const y=drops[i]*FS;const ch=CHARS[Math.floor(Math.random()*CHARS.length)];cx.fillStyle='rgba(0,255,136,0.95)';cx.fillText(ch,i*FS,y);cx.fillStyle='rgba(0,229,255,0.35)';cx.fillText(CHARS[Math.floor(Math.random()*CHARS.length)],i*FS,y-FS);cx.fillStyle='rgba(0,229,255,0.12)';cx.fillText(CHARS[Math.floor(Math.random()*CHARS.length)],i*FS,y-FS*2);if(y>cv.height&&Math.random()>0.97)drops[i]=-10;drops[i]+=speeds[i];}}
  resize();setInterval(draw,40);window.addEventListener('resize',resize);
})();
let startTime=null,mode='fixed',es=null;
function connect(){if(es){es.close();}es=new EventSource('/events');es.onopen=()=>setConn(true);es.onerror=()=>{setConn(false);};es.onmessage=e=>{try{handle(JSON.parse(e.data));}catch(_){}};}
function setConn(ok){document.getElementById('connDot').className='conn-dot'+(ok?' ok':'');document.getElementById('connText').textContent=ok?'connected':'connecting...';}
function handle(msg){if(msg.type==='init'){applyStats(msg.stats);if(msg.config)applyConfig(msg.config);if(msg.history&&msg.history.length){document.getElementById('feed').innerHTML='';msg.history.slice().reverse().forEach(addRow);}setConn(true);}else if(msg.type==='trade'){addRow(msg);if(msg.status==='skipped'||msg.status==='error'){clog('['+msg.time+'] SKIP: '+(msg.outcome||'')+(msg.market?' — '+msg.market:'')+(msg.detail?' → '+msg.detail:''));}}else if(msg.type==='status'){setStatus(msg.status);}else if(msg.type==='stats'){applyStats(msg.stats);}else if(msg.type==='log'){clog('['+msg.time+'] '+msg.message);}else if(msg.type==='ping'){}}
function applyStats(s){if(!s)return;document.getElementById('mCopied').textContent=s.trades_copied||0;document.getElementById('mSpent').textContent='$'+(+(s.total_spent_usdc||0)).toFixed(2);setStatus(s.status);startTime=s.uptime_start?s.uptime_start*1000:null;const mBal=document.getElementById('mBalance');const bal=s.balance_usdc!=null?+s.balance_usdc:null;if(mBal)mBal.textContent=bal!=null?'$'+bal.toFixed(2):'—';const mPort=document.getElementById('mPortfolio');const pos=s.portfolio_usdc!=null?+s.portfolio_usdc:null;if(mPort)mPort.textContent=pos!=null?'$'+pos.toFixed(2):'—';const mTotal=document.getElementById('mTotal');const mSub=document.getElementById('mTotalSub');if(mTotal){if(bal!=null&&pos!=null){const total=bal+pos;mTotal.textContent='$'+total.toFixed(2);if(mSub)mSub.textContent='cash $'+bal.toFixed(2)+' + positions $'+pos.toFixed(2);}else if(bal!=null){mTotal.textContent='$'+bal.toFixed(2);}else{mTotal.textContent='—';}}const mPnl=document.getElementById('mPnl');if(mPnl){if(s.pnl_usdc!=null){const v=+s.pnl_usdc;mPnl.textContent=(v>=0?'+$':'-$')+Math.abs(v).toFixed(2);mPnl.style.color=v>0?'var(--green)':v<0?'var(--red)':'var(--text)';mPnl.style.textShadow=v>0?'var(--glow-g)':v<0?'var(--glow-r)':'none';}else{mPnl.textContent='—';mPnl.style.color='var(--muted)';}}}
function applyConfig(c){document.getElementById('cfgWallet').value=c.wallet||'';const wst=document.getElementById('walletStatus');if(wst){wst.textContent=c.wallet?('✓ Saved: '+c.wallet.slice(0,10)+'...'):'No wallet saved yet';wst.style.color=c.wallet?'var(--green)':'var(--muted)';}if(c.mode==='capital_percent'){setMode('percent');document.getElementById('cfgPct').value=(c.position_percent*100).toFixed(1);}else{setMode('fixed');document.getElementById('cfgFixed').value=c.fixed_usdc;}updateModeStatus();if(c.lookback)document.getElementById('cfgLookback').value=c.lookback;document.getElementById('cfgCooldown').value=c.cooldown||0;document.getElementById('cfgFunder').value=c.funder||'';const fst=document.getElementById('funderStatus');if(fst){fst.textContent=c.funder?('✓ Saved: '+c.funder.slice(0,10)+'...'):'No address saved yet';fst.style.color=c.funder?'var(--green)':'var(--muted)';}const pkField=document.getElementById('cfgPrivKey');if(pkField&&c.has_private_key){pkField.value='●'.repeat(66);pkField.dataset.saved='1';}const pkStatus=document.getElementById('pkStatus');if(pkStatus){pkStatus.textContent=c.has_private_key?'✓ Private key saved':'No private key saved yet';pkStatus.style.color=c.has_private_key?'var(--green)':'var(--red)';}}
function setMode(m){mode=m;const isPct=m==='percent';document.getElementById('toggleTrack').classList.toggle('pct',isPct);document.getElementById('mFixed').className='toggle-label '+(isPct?'inactive':'active');document.getElementById('mPct').className='toggle-label '+(isPct?'active':'inactive');document.getElementById('rowFixed').style.display=isPct?'none':'';document.getElementById('rowPct').style.display=isPct?'':'none';updateModeStatus();}
function setModeAndSave(m){setMode(m);autoSaveConfig();}
function updateModeStatus(){const el=document.getElementById('modeStatus');if(!el)return;if(mode==='fixed'){const v=parseFloat(document.getElementById('cfgFixed').value)||5;el.textContent='MODE: Fixed $'+v.toFixed(2)+' per trade';}else{const v=parseFloat(document.getElementById('cfgPct').value)||2;el.textContent='MODE: '+v.toFixed(1)+'% of live cash balance per trade';}}
function setStatus(s){const badge=document.getElementById('badge');badge.className='badge '+(s==='running'?'running':'stopped');document.getElementById('badgeText').textContent=s.toUpperCase();document.getElementById('btnStart').disabled=s==='running';document.getElementById('btnStop').disabled=s!=='running';if(s!=='running'){startTime=null;document.getElementById('uptime').textContent='';}}
function addRow(t){if(!['filled','matched'].includes(t.status||'skipped'))return;const feed=document.getElementById('feed');const empty=feed.querySelector('.feed-empty');if(empty)empty.remove();const row=document.createElement('div');row.className='trow '+(t.status||'skipped');const sc=t.side==='BUY'?'t-buy':'t-sell';function cell(cls,text,title){const s=document.createElement('span');s.className=cls;s.textContent=text||'';if(title)s.title=title;return s;}row.appendChild(cell('t-time',t.time));row.appendChild(cell(sc,t.side));row.appendChild(cell('t-out',t.outcome,t.outcome));row.appendChild(cell('t-mkt',t.market,t.market));row.appendChild(cell('t-amt',t.usdc>0?'$'+t.usdc.toFixed(2):''));feed.insertBefore(row,feed.firstChild);while(feed.children.length>60)feed.removeChild(feed.lastChild);}
function clog(msg){const c=document.getElementById('con');const d=document.createElement('div');d.textContent=msg;c.appendChild(d);c.scrollTop=c.scrollHeight;while(c.children.length>150)c.removeChild(c.firstChild);}
function toast(msg,err){const el=document.createElement('div');el.className='toast'+(err?' err':'');el.textContent=msg;document.body.appendChild(el);setTimeout(()=>el.remove(),3500);}

async function autoSaveCreds(){const pkField=document.getElementById('cfgPrivKey');if(pkField.dataset.saved==='1')return;const pk=pkField.value.trim();const pkStatus=document.getElementById('pkStatus');if(!pk){try{await fetch('/api/credentials',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({clear:true})});if(pkStatus){pkStatus.textContent='Credentials cleared';pkStatus.style.color='var(--muted)';}}catch(_){}return;}if(pkStatus){pkStatus.textContent='Saving...';pkStatus.style.color='var(--muted)';}const funder=document.getElementById('cfgFunder').value.trim();try{const r=await fetch('/api/credentials',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({private_key:pk,funder})});const j=await r.json();if(j.ok){pkField.value='●'.repeat(66);pkField.dataset.saved='1';if(pkStatus){pkStatus.textContent=j.derived?'✓ Keys derived & ready':'✓ Key saved (derive on start)';pkStatus.style.color=j.derived?'var(--green)':'var(--yellow)';}if(j.derive_error&&pkStatus){pkStatus.textContent='⚠ Key saved — derivation failed: '+j.derive_error;pkStatus.style.color='var(--yellow)';}}else{if(pkStatus){pkStatus.textContent='✗ Error: '+(j.error||'unknown');pkStatus.style.color='var(--red)';}}}catch(_){if(pkStatus){pkStatus.textContent='✗ Save failed';pkStatus.style.color='var(--red)';}}}
function _configBody(){return{wallet:document.getElementById('cfgWallet').value.trim(),mode,fixed_usdc:parseFloat(document.getElementById('cfgFixed').value)||5,position_percent:(parseFloat(document.getElementById('cfgPct').value)||2)/100,lookback:parseInt(document.getElementById('cfgLookback').value)||300,cooldown:parseInt(document.getElementById('cfgCooldown').value)||0,funder:document.getElementById('cfgFunder').value.trim()};}
async function autoSaveConfig(){try{const body=_configBody();const r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(j.ok){const wst=document.getElementById('walletStatus');if(wst){wst.textContent=j.saved_wallet?('✓ Saved: '+j.saved_wallet.slice(0,10)+'...'):'✓ Saved';wst.style.color='var(--green)';}const fst=document.getElementById('funderStatus');if(fst){const funder=body.funder||'';fst.textContent=funder?('✓ Saved: '+funder.slice(0,10)+'...'):'✓ Funder cleared';fst.style.color=funder?'var(--green)':'var(--muted)';}const fixedSt=document.getElementById('fixedStatus');if(fixedSt&&mode==='fixed'){fixedSt.textContent='✓ Saved: $'+(body.fixed_usdc||5).toFixed(2)+' per trade';fixedSt.style.color='var(--green)';}const pctSt=document.getElementById('pctStatus');if(pctSt&&mode==='percent'){pctSt.textContent='✓ Saved: '+((body.position_percent||0.02)*100).toFixed(1)+'% per trade';pctSt.style.color='var(--green)';}const lkSt=document.getElementById('lookbackStatus');if(lkSt){lkSt.textContent='✓ Saved: '+(body.lookback||300)+'s';lkSt.style.color='var(--green)';}const cdSt=document.getElementById('cooldownStatus');if(cdSt){const cd=body.cooldown||0;cdSt.textContent=cd>0?('✓ Saved: '+cd+'s cooldown per market'):'✓ Saved: off';cdSt.style.color='var(--green)';}}}catch(_){}}
async function startBot(){const wallet=document.getElementById('cfgWallet').value.trim();if(!wallet||wallet.length!==42||!wallet.startsWith('0x')){toast('Enter a valid 0x wallet address to copy first.',true);document.getElementById('cfgWallet').focus();return;}try{await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(_configBody())});}catch(_){}try{const r=await fetch('/api/start',{method:'POST'});const j=await r.json();if(j.ok){setStatus('running');}else{toast('Error: '+j.error,true);}}catch(e){toast('Start failed.',true);}}
async function stopBot(){setStatus('stopped');try{await fetch('/api/stop',{method:'POST'});}catch(_){}}
setInterval(()=>{if(!startTime)return;const s=Math.floor((Date.now()-startTime)/1000);const h=String(Math.floor(s/3600)).padStart(2,'0');const m=String(Math.floor((s%3600)/60)).padStart(2,'0');const ss=String(s%60).padStart(2,'0');document.getElementById('uptime').textContent=h+':'+m+':'+ss;},1000);
connect();
</script>
</body>
</html>"""
